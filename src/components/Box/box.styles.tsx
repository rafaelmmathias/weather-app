import styled from "styled-components";
import {
  color,
  space,
  border,
  position,
  flexbox,
  layout,
  grid,
  backgroundImage,
  backgroundSize,
} from "styled-system";

export const BoxContainer = styled.div`
  ${color}
  ${space}
  ${border}
  ${position}
  ${flexbox}
  ${layout}
  ${grid}
  ${backgroundImage}
  ${backgroundSize}
`;
