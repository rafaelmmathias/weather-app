import styled from "styled-components";
import {
  color,
  space,
  border,
  position,
  flexbox,
  layout,
  grid,
} from "styled-system";

export const BoxContainer = styled.div`
  ${color}
  ${space}
  ${border}
  ${position}
  ${flexbox}
  ${layout}
  ${grid}
`;
